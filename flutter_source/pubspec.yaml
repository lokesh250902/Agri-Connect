name: agri_voice_ai
description: Multilingual AI agriculture assistant.
publish_to: "none"
version: 0.1.0+1

environment:
  sdk: ">=3.5.0 <4.0.0"

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.8
  record: ^6.0.0
  http: ^1.2.0
  audioplayers: ^6.0.0
  path_provider: ^2.1.5
  image_picker: ^1.1.2
  geolocator: ^13.0.0
  cross_file: ^0.3.4+2
  http_parser: ^4.0.2

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0

flutter:
  uses-material-design: true
